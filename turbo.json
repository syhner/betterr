{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "inputs": ["src/**/*.ts"],
      "outputs": ["dist"]
    },
    "format": {
      "inputs": ["*.json", "*.md", "*.ts", "*.yml"]
    },
    "lint": {
      "inputs": ["src/**/*.ts", "tests/**/*.ts"]
    },
    "test": {
      "inputs": ["src/**/*.ts", "tests/**/*.ts"]
    }
  }
}
